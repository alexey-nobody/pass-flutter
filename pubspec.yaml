name: passkit
description: Plugin for work with PassKit Framework (Apple Pay and Wallet)
version: 1.0.0
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  archive: ^2.0.11
  dio: ^3.0.8
  path_provider: ^1.6.0
  uuid: ^2.0.4

  json_annotation: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^3.2.5
  build_runner: ^1.7.4

flutter:
  plugin:
    androidPackage: com.example.passkit
    pluginClass: PasskitPlugin
